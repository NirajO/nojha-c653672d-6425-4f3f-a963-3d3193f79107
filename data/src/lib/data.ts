// Roles
export enum Role {
  OWNER = 'OWNER',
  ADMIN = 'ADMIN',
  VIEWER = 'VIEWER',
}

// Task Status
export enum TaskStatus {
  TODO = 'TODO',
  IN_PROGRESS = 'IN_PROGRESS',
  DONE = 'DONE',
}

// Task Category
export enum TaskCategory {
  WORK = 'WORK',
  PERSONAL = 'PERSONAL',
}